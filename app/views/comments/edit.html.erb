<%= form_for(@comment, html: {class: "app_form", onsubmit: "return validate_comment();"}) do |frm| %>
  <%= frm.text_area :content %> <br>
  <%= frm.submit "Save changes", class: "app_button_smaller edit_button" %>
<% end %>
